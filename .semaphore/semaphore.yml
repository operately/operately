version: v1.0
name: Operately — Build & Test
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004

auto_cancel:
  running:
    when: "branch != 'main'"

blocks:
  - name: Pre-Flight-Check
    dependencies: []
    task:
      jobs:
        - name: PR Name Check
          commands:
            - checkout
            - docker pull operately/operately-dev:latest
