version: v1.0
name: Release
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Release
    task:
      jobs:
        - name: "Release"
          commands:
            - checkout
            - elixir rel/single-host/build.ex 0.0.1
            - echo $RELEASE_TYPE
