import { BaseButtonProps } from "./UnstalyedButton";
export declare function calcClassName(props: BaseButtonProps, { normal, loading, always }: {
    normal: string;
    loading: string;
    always: string;
}): string;
