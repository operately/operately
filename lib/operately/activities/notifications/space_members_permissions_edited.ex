defmodule Operately.Activities.Notifications.SpaceMembersPermissionsEdited do
  def dispatch(_activity) do
    # Notification dispatcher for SpaceMembersPermissionsEdited not implemented yet
    {:ok, []}
  end
end
