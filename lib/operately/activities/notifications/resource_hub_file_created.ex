defmodule Operately.Activities.Notifications.ResourceHubFileCreated do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubFileCreated not implemented yet
    {:ok, []}
  end
end
