defmodule Operately.Activities.Notifications.ResourceHubDocumentDeleted do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubDocumentDeleted not implemented yet
    {:ok, []}
  end
end
