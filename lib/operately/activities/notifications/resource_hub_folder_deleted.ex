defmodule Operately.Activities.Notifications.ResourceHubFolderDeleted do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubFolderDeleted not implemented yet
    {:ok, []}
  end
end
