defmodule Operately.Activities.Notifications.ProjectKeyResourceDeleted do
  def dispatch(_activity) do
    # Notification dispatcher for ProjectKeyResourceDeleted not implemented yet
    {:ok, []}
  end
end
