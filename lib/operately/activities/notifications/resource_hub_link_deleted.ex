defmodule Operately.Activities.Notifications.ResourceHubLinkDeleted do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubLinkDeleted not implemented yet
    {:ok, []}
  end
end
