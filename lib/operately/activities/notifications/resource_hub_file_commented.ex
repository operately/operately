defmodule Operately.Activities.Notifications.ResourceHubFileCommented do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubFileCommented not implemented yet
    {:ok, []}
  end
end
