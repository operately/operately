defmodule Operately.Activities.Notifications.ResourceHubDocumentEdited do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubDocumentEdited not implemented yet
    {:ok, []}
  end
end
