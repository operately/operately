defmodule Operately.Activities.Notifications.ResourceHubFileDeleted do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubFileDeleted not implemented yet
    {:ok, []}
  end
end
