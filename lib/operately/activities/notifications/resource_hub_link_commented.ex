defmodule Operately.Activities.Notifications.ResourceHubLinkCommented do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubLinkCommented not implemented yet
    {:ok, []}
  end
end
