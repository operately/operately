defmodule Operately.Activities.Notifications.ResourceHubParentFolderEdited do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubParentFolderEdited not implemented yet
    {:ok, []}
  end
end
