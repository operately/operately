<%= title("#{short_name(@author)} connected the #{@project.name} project to the #{@goal.name} goal") %>

<%= spacer() %>

<%= row do %>
  <%= cta_button(OperatelyEmail.project_url(@project.id), "View Project") %>
<% end %>
