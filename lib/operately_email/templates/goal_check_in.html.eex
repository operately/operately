<%= title("#{short_name(@author)} update the progress for the #{@goal.name} goal") %>

<%= spacer() %>

<%= row do %>
  <%= rich_text(@update.content["message"]) %>
<% end %>

<%= spacer() %>

<%= row do %>
  <%= cta_button(OperatelyEmail.goal_check_in_url(@goal.id, @update.id), "View Update") %>
<% end %>
