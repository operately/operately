<%= title("#{short_name(@author)} acknowledged your check-in for #{@project.name} project") %>

<%= spacer() %>

<%= row do %>
  <%= cta_button(OperatelyEmail.project_check_in_url(@project.id, @update.id), "View Check-In") %>
<% end %>
