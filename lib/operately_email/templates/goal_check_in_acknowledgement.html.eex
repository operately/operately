<%= title("#{short_name(@author)} acknowledged the progress update for #{@goal.name} goal") %>

<%= spacer() %>

<%= row do %>
  <%= cta_button(OperatelyEmail.goal_check_in_url(@goal.id, @update.id), "View Update") %>
<% end %>
