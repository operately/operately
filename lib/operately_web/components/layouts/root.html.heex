<!DOCTYPE html>
<html lang="en" style="scrollbar-gutter: stable;">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix=" · Operately">
      <%= assigns[:page_title] %>
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />

    <script>
      // Disable annoying Download the React DevTools for a better
      // development experience: https://fb.me/react-devtools message

      (function() {
        var info = console.info

        console.info = function (message) {
          if (!/Download the React DevTools/.test(message)) info.apply(console, arguments)
        }
      })()

      window.companyID = "<%= Operately.Companies.get_company_id() %>";
    </script>

    <style>
      @import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;700&display=swap');
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap');

      body {
        font-family: 'Inter', sans-serif;
      }
    </style>

    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="bg-new-dark-1 antialiased overscroll-none" style="background: linear-gradient(49deg, rgba(22,22,22,1) 0%, rgba(33,33,33,1) 77%, rgba(22,22,22,1) 100%); min-height: 100vh;">
    <%= @inner_content %>
  </body>
</html>
