Updates from the last <%= @window_minutes %> minutes

<%= if Enum.empty?(@parent_groups) do %>
You're all caught up.
No new updates came in during the last <%= @window_minutes %> minutes.
<% else %>
<%= for group <- @parent_groups do %>
<%= group.parent_name %> (<%= length(group.items) %> <%= if length(group.items) == 1, do: "update", else: "updates" %>)
<%= for item <- group.items do %>
  - <%= item.actor_name %>: <%= item.headline %>
<%= if item.excerpt_text do %>
    <%= item.excerpt_text %>
<% end %>
    <%= item.item_url %>

<% end %>
<% end %>
<% end %>

View all notifications: <%= @notifications_url %>
