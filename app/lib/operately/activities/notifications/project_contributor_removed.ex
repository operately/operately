defmodule Operately.Activities.Notifications.ProjectContributorRemoved do
  def dispatch(_activity) do
    # Notification dispatcher for ProjectContributorRemoved not implemented yet
    {:ok, []}
  end
end
