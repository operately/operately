defmodule Operately.Activities.Notifications.SpacePermissionsEdited do
  def dispatch(_activity) do
    # Notification dispatcher for SpacePermissionsEdited not implemented yet
    {:ok, []}
  end
end
