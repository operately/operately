defmodule Operately.Activities.Notifications.ProjectRetrospectiveEdited do
  def dispatch(_activity) do
    # Notification dispatcher for ProjectRetrospectiveEdited not implemented yet
    {:ok, []}
  end
end
