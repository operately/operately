defmodule Operately.Activities.Notifications.ResourceHubFolderCopied do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubFolderCopied not implemented yet
    {:ok, []}
  end
end
