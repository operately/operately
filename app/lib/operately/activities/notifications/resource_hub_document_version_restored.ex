defmodule Operately.Activities.Notifications.ResourceHubDocumentVersionRestored do
  def dispatch(_activity) do
    # Notification dispatcher for ResourceHubDocumentVersionRestored not implemented yet
    {:ok, []}
  end
end
